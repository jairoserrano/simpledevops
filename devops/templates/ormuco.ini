[uwsgi]
socket = {{webapps_dir}}/{{app_name}}/var/{{app_name}}.sock
chmod-socket = 775
chdir = {{webapps_dir}}/{{app_name}}/src
master = true
module = app:app
processes = 1
threads = 1
plugin = python
